<% if @post.favourited_by?(current_user) %>
  <%= link_to "Remove from favourites", post_favourite_path(@post, @post.favourites_for(current_user)), remote: true, method: :delete %>
<% else %>
  <%= link_to "Add to favourites", post_favourites_path(@post), remote: true, method: :post %>
<% end %>
