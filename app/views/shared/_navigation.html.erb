<nav class="navbar navbar-default navbar-fixed-top">
  <div class="container-fluid">
    <div class="navbar-header">
      <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar-collapse">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href="<%= root_path %>">LeBlog</a>
    </div>

    <div class="collapse navbar-collapse" id="navbar-collapse">
      <ul class="nav navbar-nav">
        <li class="<%= 'active' if current_page?(about_path) %>"><a href="<%= about_path %>">About</a></li>
        <li class="<%= 'active' if current_page?(posts_path) %>"><a href="<%= posts_path %>">View Posts</a></li>
        <li class="<%= 'active' if current_page?(new_post_path) %>"><a href="<%= new_post_path %>">Add New Post</a></li>
      </ul>

      <div class="navbar-form navbar-left" style="margin-top: -10px;">
        <div class="form-group">
          <%= form_tag(posts_path, :method => "get", id: "search-form") do %>
          <%= text_field_tag :search, params[:search], placeholder: "Search Posts", class:"form-control" %>
          <%= submit_tag "Search", class:"btn btn-default"%>
        </div>
          <% end %>
      </div>

      <ul class="nav navbar-nav navbar-right">
        <% if user_signed_in? %>
          <li class="navbar-text"><%= current_user.first_name %></li>
          <li class="<%= 'active' if current_page?(user_favourite_posts_path) %>"><a href="<%= user_favourite_posts_path %>">Favourites</a></li>
          <li><%= link_to "Logout", sessions_path, method: :delete,
                      data: {confirm: "are you sure you want to log out?"}%></li>
        <% else %>
          <li class="<%= 'active' if current_page?(new_session_path) %>"><a href="<%= new_session_path %>">Login</a></li>
          <li class="<%= 'active' if current_page?(new_user_path) %>"><a href="<%= new_user_path %>">Sign Up</a></li>
        <% end %>
      </ul>
    </div>
  </div>
</nav>
